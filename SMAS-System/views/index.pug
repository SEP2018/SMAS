extends layout

block content
  h1= title
  p Welcome to #{title}
  a(href='/appointments/create')
    button(style='largeButton') Make Appointment
    a(href='/appointments/delete')
      button(style='largeButton') Cancel Appointment